# Replace example.com with your root domain and adjust the filesystem path.
# Serves https://example.com/ directly; site/ is NOT a URL path prefix.
# Merge into your existing Caddyfile.
example.com {
    root * /srv/hg-identifiers/site
    encode zstd gzip
    header Cache-Control "no-cache"
    file_server
}
