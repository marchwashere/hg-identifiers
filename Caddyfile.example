# Replace the domain and path. Merge into your existing Caddyfile.
identifiers.example.com {
    root * /srv/hg-identifiers/site
    encode zstd gzip
    header Cache-Control "no-cache"
    file_server
}
